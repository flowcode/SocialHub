{% extends "FlowcodeDashboardBundle:Default:layout.html.twig" %}

{% block title %}SocialNetwork - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>
            <a href="{{ path('admin_socialnetwork') }}"> {{ 'SocialNetwork list'|trans({'SocialNetwork': 'SocialNetwork'}, 'SocialNetwork') }}</a>
            /
            {{ 'SocialNetwork' | trans({}, 'SocialNetwork')}}
        </h1>
    </section>
    <section class="content">    
        <div class="box"  id='showSocialNetwork'>
            <div class="box-header">
                <div class="box-tools  pull-right">
                            {{ form_start(delete_form) }}
        {{ form_widget(delete_form) }}

        <div class="btn-group record_actions">            <button class="btn btn-link btn-sm" type="submit"> {{ 'Delete'|trans({}, 'SocialNetwork') }}</button>
            <input name="modal" type="hidden" value="0">
            
                <a class="btn btn-link  btn-sm" onclick="showEdit(true)">
                    {{ 'Edit'|trans({}, 'SocialNetwork') }}
                </a>
</div>
            <input name="modal" type="hidden" value="0">
        {{ form_end(delete_form) }}

</div>
            </div>
            <div class="box-body">
                <table class="table table-hover record_properties">
                    <tbody>                            
                        <tr>
                            <th scope="row">{{'Name'|trans({}, 'SocialNetwork')}}</th>
                            <td>{{ socialnetwork.name }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Description'|trans({}, 'SocialNetwork')}}</th>
                            <td>{{ socialnetwork.description }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Clientid'|trans({}, 'SocialNetwork')}}</th>
                            <td>{{ socialnetwork.clientId }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Clientsecret'|trans({}, 'SocialNetwork')}}</th>
                            <td>{{ socialnetwork.clientSecret }}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Loginenabled'|trans({}, 'SocialNetwork')}}</th>
                            <td>{% if socialnetwork.loginEnabled %}<i class="fa fa-check-square-o"></i>{% else %}<i class="fa fa-square-o"></i>{% endif %}</td>
                        </tr>                            
                        <tr>
                            <th scope="row">{{'Enabled'|trans({}, 'SocialNetwork')}}</th>
                            <td>{% if socialnetwork.enabled %}<i class="fa fa-check-square-o"></i>{% else %}<i class="fa fa-square-o"></i>{% endif %}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div class="box-footer">
            
            </div>
        </div>
        <div class="box" id='editFormSocialNetwork'  style="display: none;">
            <div class="box-header">
                <div class="box-tools">
                    {{ form_start(delete_form) }}
                    {{ form_widget(delete_form) }}
                    <div class="btn-group record_actions">
                        <button class="btn btn-link btn-sm" type="submit"> {{ 'Delete'|trans({}, 'SocialNetwork') }}</button>
                        <a class="btn btn-link  btn-sm" onclick="showEdit(false)">
                        {{ 'Cancel'|trans({}, 'Account') }}
                        </a>
                    </div>
                    <input name="modal" type="hidden" value="0">
                    {{ form_end(delete_form) }}
                </div>
            </div>
            <div class="box-body">

            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <p>
                    <button class="btn-default btn" type="submit">{{ 'save'|trans | capitalize}}</button>
                </p>
            {{ form_end(edit_form) }}
            </div>
            <div class="box-footer">
            </div>
        </div>
    </section>

        <div class="btn-group record_actions"></div>

        <div id="delete_confirm" class="modal fade">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                        <h3>{{ 'Confirm delete'|trans({}, 'SocialNetwork') }}</h3>
                    </div>
                    <div class="modal-body">
                        <p>{{ 'You are about to delete an item'|trans({}, 'SocialNetwork') }}, {{ 'this procedure is irreversible'|trans({}, 'SocialNetwork') }}.</p>
                        <p>{{ 'Do you want to proceed?'|trans({}, 'SocialNetwork') }}</p>
                    </div>
                    <div class="modal-footer">
                        <button class="btn btn-danger">{{ 'Yes'|trans({}, 'SocialNetwork') }}</button>
                        <button class="btn btn-default" data-dismiss="modal">{{ 'No'|trans({}, 'SocialNetwork') }}</button>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}
{% block javascripts %}

    {{ parent() }}
    <script type="text/javascript">
        function showEdit(show){
            if(show){
                $('#showSocialNetwork').hide();
                $('#editFormSocialNetwork').show();
            }else{
                $('#showSocialNetwork').show();
                $('#editFormSocialNetwork').hide();
            }
        }
        
    </script>
{% endblock %}